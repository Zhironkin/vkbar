import ReactLoader from "react-loader-spinner";

const Loader = () => (
   <ReactLoader
      className="loader"
      type="TailSpin"
      color="#00BFFF"
      height={100}
      width={100}
   />
)

export default Loader;